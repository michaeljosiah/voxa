Labeled select for picking voices, regions, models.

```jsx
const { Select } = window.VOXA;
<Select label="Voice" options={['en-US-Ava', 'en-US-Andrew', 'en-GB-Sonia']} />
```

- Native `<select>` under the hood — reliable in prototypes.
- Same focus/hover behavior as Input.
