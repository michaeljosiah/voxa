Boolean toggle — on-state fills cyan with an ink knob.

```jsx
const { Switch } = window.VOXA;
<Switch label="Interruptions" defaultChecked />
```

- Use for instant-apply settings; use Button for actions that submit.
- Works controlled (`checked`) or uncontrolled (`defaultChecked`).
