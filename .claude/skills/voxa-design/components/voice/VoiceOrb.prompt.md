The agent's visual presence — a cyan orb that ripples when live. One orb per view, max.

```jsx
const { VoiceOrb } = window.VOXA;
<VoiceOrb size={140} live />
```

- Hero art on the website; "agent is speaking" indicator in call UIs.
- Pure CSS — no canvas/WebGL needed.
